-- Tenant-scoped wipe. Uncomment and set the workspace id, then run.
-- Never TRUNCATE all workspaces. Keeps polling units and users for that tenant.
--
-- SELECT public.zero_operational_campaign_data('a0000000-0000-0000-0000-000000000001'::uuid);
